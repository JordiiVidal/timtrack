import 'package:flutter/material.dart';

class CircleStatusCycle extends StatelessWidget {
  const CircleStatusCycle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
